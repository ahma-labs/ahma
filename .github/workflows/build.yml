name: Ahma
permissions:
  contents: read
  pages: write
  id-token: write
# CACHING STRATEGY
#
# The repo-wide Actions cache quota is 10 GiB, LRU-evicted. Every byte a job saves competes
# with every other job, so the design constraint is a *steady-state footprint*, not a per-job
# hit rate. The previous daily-rotation scheme ignored this: one full matrix run saved 23
# entries totalling ~12 GiB, so it could not hold even a single day, let alone the previous
# day it was written to fall back to. Release jobs (which save last) evicted the test caches
# that the next day's test legs were supposed to warm from. Four `sccache-test-*` tarballs
# alone accounted for ~6.8 GiB.
#
# Four rules keep it inside the quota. Change any of them and re-check the arithmetic.
#
# 1. PR runs consume cache, never produce it. Caches saved on a `pull_request` ref are
#    visible only to that one PR, yet they bill against the shared 10 GiB and evict main's
#    entries, which every PR restores from. So fast-tier.yml is restore-only. The cost is
#    that a second push to the same PR recompiles the workspace crates it changed; deps
#    still come from main's cache, which is at most one merge stale.
#
# 2. Release jobs do not cache sccache at all. They build with `--release`, so they share
#    zero sccache objects with the test matrix (sccache keys on the whole compiler
#    invocation), they run only on a version bump, and they are not on any latency-critical
#    path. Restore-only was considered and rejected: with nobody saving, the entry ages out
#    within a week and the complexity buys nothing.
#
# 3. One registry cache per (Cargo.lock, OS, arch) -- not per job family. Registry content
#    is a pure function of the lockfile, so the old -test-/-android-/-coverage-/-release-
#    namespaces stored up to 11 near-identical copies (~1.7 GiB). Lockfile-keyed also means
#    an exact hit skips the save, so unchanged deps never re-upload.
#
# 4. sccache uses restore + save with a per-run key, so it is never write-once. `actions/cache`
#    skips the save on an exact key hit, which under the old daily key froze cache contents
#    for the rest of the day: a dependency added at 09:05 was recompiled by every run until
#    midnight UTC. Splitting into `actions/cache/restore` + `actions/cache/save` keyed on
#    `<lockfile-hash>-<run_id>-<run_attempt>` makes every run save fresh and restore the
#    newest matching prefix. The save is `if: always()` so a red run still contributes its
#    compilation instead of making the retry start over.
#
# Rule 4 makes every run mint a new key, so it REQUIRES the janitor in
# .github/workflows/cache-janitor.yml, which keeps only the newest entry per sccache family.
# Without it, run-scoped keys thrash the quota harder than the daily scheme did.
#
# SCCACHE_CACHE_SIZE is the sizing knob, and the name matters: **`SCCACHE_MAX_SIZE` is not a
# real sccache variable**. sccache reads only `SCCACHE_CACHE_SIZE` (src/config.rs, and nothing
# named SCCACHE_MAX_SIZE appears in that file at all); an unrecognised name silently leaves the
# default of 10 GiB. This repo set `SCCACHE_MAX_SIZE` for its whole history, so no cap was ever
# enforced and every local cache was free to grow toward 10 GiB. That -- not the daily rotation
# -- is the root cause of the original quota saturation, and it also refutes the earlier reading
# that the Linux leg's 2.05 GiB upload showed it "pinned against a 2G cap": it was simply
# unbounded. Verified against the measured post-#560 run, where legs overshot their nominal caps
# (Linux/X64 2276 MiB vs 1 GiB, coverage 1121 vs 800, android 736 vs 600).
#
# Caps therefore bind for the FIRST time as of this change. The consequence to watch: the four
# test legs have a natural working set of ~2 GiB and are now capped at 1 GiB, so sccache will
# evict roughly half. Rust hit rates were 85-98% when uncapped; if the next main run shows a
# material drop there is ~3 GiB of headroom in the budget to raise the cap. Raising it is not
# free -- multiply by the number of saving legs and compare to 10 GiB.
#
# ----------------------------------------------------------------------------------------------
# IN PROGRESS: ghac spike on job-coverage (#562). Everything above still describes the other jobs.
#
# `job-coverage` alone no longer uses the tarball model. It uses sccache's native GitHub Actions
# backend (`SCCACHE_GHA_ENABLED=on`), which stores each compilation object as its own Actions
# cache entry instead of tarring SCCACHE_DIR into one. For that one job, rule 4, the
# SCCACHE_CACHE_SIZE cap and the janitor's keep-N row are all inert: `storage_from_config` returns
# the ghac backend and never constructs the disk cache, so there is no local directory to bound
# and no single entry to supersede. It was picked as the guinea pig because it is a single leg on
# nobody's critical path whose live working set was characterised precisely by #564.
#
# Baseline to beat, from run 31404259524, the last full tarball run:
#   80.69% Rust hit rate, 1 GiB restored / 1103 MiB saved, 11.7 min wall clock.
# That hit rate is mid-recovery from the 800M incident below, not steady state; the pre-incident
# figure was 95.74%. The 11.7 min was already back to pre-incident, so **wall clock decides this,
# not hit rate**. ghac issues roughly one HTTP request per compilation unit against a rate-limited
# service; the tarball restore it replaces moved 2.2 GiB at ~260 MB/s in about 9s.
#
# Every ghac failure mode is silent, which is why the verify step in job-coverage asserts rather
# than prints. Read against sccache v0.17.0 + opendal 0.55.0:
#   - Rate limited: documented as "the build will continue, but the storage might not be
#     performed".
#   - Write probe fails: `RemoteStorage::check` tolerates the error and returns
#     `CacheMode::ReadOnly`, after which every put returns "Cannot write to read-only storage".
#     That does land in `cache_write_errors`, which is what makes it detectable at all.
#   - Read fails: `RemoteStorage::get` downgrades any unexpected error to `Cache::Miss` with a
#     `warn!`. A dead cache is indistinguishable from a cold one except by hit rate.
#   - Credentials missing: sccache keeps using the disk cache and the job silently keeps
#     measuring the model this spike is supposed to be replacing. Hence the `cache_location`
#     assertion.
#
# Reverting is one env var plus three steps: drop SCCACHE_GHA_ENABLED and the credential-export
# step from job-coverage, restore its Restore/Save sccache steps and its `SCCACHE_CACHE_SIZE:
# 1300M`. The janitor still keeps the last `sccache-coverage-*` tarball for exactly that reason,
# so a revert is not a cold start.
#
# Rollout gate: migrate the other families only after >= 3 consecutive main runs with
# equal-or-better wall clock, zero cache errors, AND no growth in repo-wide cache usage. ghac
# objects bill against the same 10 GiB quota as the tarballs, but unlike a tarball they are NOT
# capped by anything this repo controls -- GitHub's LRU and its 7-day-unused eviction are the only
# bounds, so the steady state is "live set + one week of churn" rather than a number in the budget
# table. cache-janitor.yml already prints repo-wide usage in its step summary after every run of
# this workflow; that is the number to watch.
#
# If it wins: delete cache-janitor.yml, delete the SCCACHE_CACHE_SIZE budget table, and reduce
# this note to the ghac setup plus rule 1, which `SCCACHE_GHA_RW_MODE=READ_ONLY` would then
# enforce directly in fast-tier.yml rather than by the convention of omitting a save step.
# ----------------------------------------------------------------------------------------------
#
# Manual cache inspection / reset:
#   gh cache list --limit 100
#   gh api repos/:owner/:repo/actions/cache/usage
#   gh cache delete --all
#
# Avoid pull requests- they cause needless double builds. Instead rebase from 'main'
# to your branch to give priority to your changes. Then rebase from your branch back
# to 'main', test, then push.

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  # Ref-aware cancellation: cancel superseded runs on merge-queue / dispatch refs (iterative
  # work), but NEVER cancel a run on main/production — each main push is a distinct
  # squash-merge that may publish a release, so it must always finish.
  cancel-in-progress: ${{ github.ref != 'refs/heads/main' && github.ref != 'refs/heads/production' }}

# TIERED CI (see .github/workflows/fast-tier.yml):
#   - Pull requests run the FAST tier only (Linux fmt + clippy + nextest `smoke`), ~5 min.
#   - This full cross-platform matrix runs at the MERGE GATE via the merge queue
#     (`merge_group`) — once per landed feature, not on every iteration push — and on
#     every push to main/production. The release binaries are built + published only when
#     the push bumps Cargo.toml to an as-yet-unreleased version (see job-release-gate): an
#     ordinary land runs the test matrix + coverage but skips the 6-platform release build.
# This is why `pull_request` is intentionally NOT a trigger here: the full matrix is the
# gate to LAND, the fast tier is the gate to ITERATE.
on:
  push:
    branches: [main, production]
  merge_group:
  workflow_dispatch:

env:
  RUSTUP_MAX_RETRIES: 10
  CARGO_TERM_COLOR: always
  CARGO_PAGER: cat
  CARGO_INCREMENTAL: 0
  CARGO_PROFILE_TEST_DEBUG: 0
  CARGO_NET_RETRY: 10
  CARGO_PROFILE_DEV_BUILD_OVERRIDE_DEBUG: false
  RUSTFLAGS: -C debuginfo=0 --cfg reqwest_unstable # Do not produce debug symbols; enable HTTP/3 (QUIC) in reqwest
  RUST_BACKTRACE: 1
  IS_DEV_BUILD: "true" # Set "true" to speed up builds for CI testing
  RUSTC_WRAPPER: sccache
  SCCACHE_DIR: ${{ github.workspace }}/.sccache
  # 1 GiB is the per-leg budget for the 4-leg test matrix: 4 x 1 GiB is already 40% of the
  # 10 GiB repo quota. Jobs that save less get a lower cap at job level. See the caching
  # strategy note at the top of this file before changing this.
  SCCACHE_CACHE_SIZE: 1G
  SCCACHE_DIRECT: "true"
  VSCMD_SKIP_SENDTELEMETRY: 1
jobs:
  job-cargo-nextest:
    name: Cargo Nextest (${{ matrix.os }})
    defaults:
      run:
        shell: bash
    strategy:
      fail-fast: false
      # Free OSS allows up to 20 concurrent jobs; capping at 2 forced Windows nextest to
      # queue ~9-10 min behind ubuntu+macos before even starting. Run the whole matrix at
      # once so the critical path is the slowest single leg (Windows), not the sum of waves.
      max-parallel: 4
      matrix:
        include:
          - os: ubuntu-latest
            test_mode: full
          - os: ubuntu-24.04-arm
            test_mode: smoke   # ARM64 runners are resource-constrained; compile-only smoke
          - os: macos-latest
            test_mode: full
          - os: windows-latest
            test_mode: full
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v6
        with:
          ref: ${{ github.event.pull_request.head.sha || github.sha }}
          fetch-depth: 2
          token: ${{ github.token }}
          persist-credentials: true

      # Keeping antivirus out of the build tree is a speed optimization, not a
      # correctness requirement. Add-MpPreference is a live call to the Defender
      # service, which on hosted runners is sometimes disabled or unreachable
      # (WMI 0x800106ba) -- so it must never be able to fail the job.
      - name: Configure Windows Defender exclusions (best effort)
        if: runner.os == 'Windows'
        shell: powershell
        continue-on-error: true
        run: |
          $paths = @("${{ github.workspace }}", "$env:USERPROFILE\.cargo", "$env:TEMP")
          foreach ($path in $paths) {
            try {
              Add-MpPreference -ExclusionPath $path -ErrorAction Stop
              Write-Host "Defender exclusion added: $path"
            } catch {
              # Collapse newlines: a raw newline truncates a ::warning:: annotation.
              $reason = ($_.Exception.Message -replace '\s+', ' ').Trim()
              Write-Host "::warning::Defender exclusion skipped for ${path}: ${reason} -- builds may be slower, but correctness is unaffected."
            }
          }

      # Shared across every job in this workflow (rule 3 in the caching note above): registry
      # content is a pure function of (Cargo.lock, OS, arch), so job family is not part of the
      # key. `registry/src` is deliberately NOT cached -- cargo re-extracts it from
      # `registry/cache`, and it is the bulk of the payload (1.5 GiB of extracted sources
      # against 274 MiB of .crate archives). `~/.cargo/bin` is deliberately NOT cached either:
      # a restored stale binary satisfies taiki-e/install-action's presence check, silently
      # pinning an old nextest/sccache.
      - name: Cache Cargo registry and git
        uses: actions/cache@v5
        with:
          path: |
            ~/.cargo/registry/index
            ~/.cargo/registry/cache
            ~/.cargo/git/db
          key: cargo-registry-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('Cargo.lock') }}
          restore-keys: |
            cargo-registry-${{ runner.os }}-${{ runner.arch }}-

      # Restore + save split, not `actions/cache` (rule 4 above): a single `actions/cache` with
      # a stable key skips the save on an exact hit, freezing cache contents. run_attempt is in
      # the key because run_id alone is identical across re-runs of the same run.
      - name: Restore sccache
        id: sccache
        uses: actions/cache/restore@v5
        with:
          path: ${{ github.workspace }}/.sccache
          key: sccache-test-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('Cargo.lock') }}-${{ github.run_id }}-${{ github.run_attempt }}
          restore-keys: |
            sccache-test-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('Cargo.lock') }}-
            sccache-test-${{ runner.os }}-${{ runner.arch }}-

      - uses: actions-rust-lang/setup-rust-toolchain@v1
        with:
          components: clippy, rustfmt
          rustflags: ""
          cache: false

      - name: Configure deterministic tooling env
        run: |
          git config --global core.pager cat
          git config --global fetch.recurseSubmodules false
          {
            echo "PAGER=cat"
            echo "LC_ALL=C"
            echo "LANG=C"
          } >> "$GITHUB_ENV"

      - name: Preflight workspace crate roots
        run: |
          set -euo pipefail
          missing=0
          while IFS= read -r manifest; do
            crate_dir="$(dirname "$manifest")"
            # Workspace root manifests may not define a package.
            if ! grep -q "^\[package\]" "$manifest"; then
              continue
            fi
            if [ ! -f "$crate_dir/src/lib.rs" ] && [ ! -f "$crate_dir/src/main.rs" ]; then
              echo "Missing crate root in $crate_dir (expected src/lib.rs or src/main.rs)"
              missing=1
            fi
          done < <(find . -name Cargo.toml -not -path "./target/*")

          if [ "$missing" -ne 0 ]; then
            echo ""
            echo "Preflight failed: one or more crates are missing source roots."
            echo "This often happens when required files exist locally but are untracked."
            exit 1
          fi

      - name: Install sccache
        uses: taiki-e/install-action@v2
        with:
          tool: sccache

      - name: Install cargo-nextest
        if: matrix.test_mode == 'full'
        uses: taiki-e/install-action@v2
        with:
          tool: nextest

      - name: Add Cargo Bin to PATH
        run: echo "$HOME/.cargo/bin" >> "$GITHUB_PATH"

      # Fail fast on formatting before any expensive compile. The PR fast tier already runs
      # this, but a direct push to main (e.g. the release version-bump commit) bypasses the
      # fast tier — this is main's fmt backstop. <1s; Linux only (rustfmt is platform-agnostic).
      - name: Cargo fmt --check
        if: runner.os == 'Linux' && runner.arch == 'X64'
        run: cargo fmt --all --check

      - name: Build ahma binary (required by integration tests)
        if: matrix.test_mode == 'full'
        run: cargo build --locked --no-default-features -p ahma_bin

      - name: Cargo Nextest (excluding Android/Kotlin tests)
        if: matrix.test_mode == 'full'
        timeout-minutes: 40
        run: |
          echo "--- cargo --version" && cargo --version
          echo "--- cargo nextest --version" && cargo nextest --version
          echo "--- Running cargo nextest run --profile ci"
          # --no-fail-fast: surface EVERY failing test in a single run instead of
          # cancelling at the first failure.  Without it, a platform with several
          # independent failures (notably Windows) only reveals one per CI round,
          # turning stabilization into a slow whack-a-mole across many pushes.
          cargo nextest run --profile ci --locked --no-default-features --no-fail-fast -E 'not (test(kotlin) or test(android))'

      - name: Cargo Nextest (AGPL product crates)
        if: matrix.test_mode == 'full'
        timeout-minutes: 20
        run: |
          echo "--- Testing AGPL product crates"
          cargo nextest run --profile ci --locked \
            -p ahma_vault -p ahma_cluster \
            -E 'not (test(kotlin) or test(android))'

      - name: Cargo Nextest (Landlock red-team, Linux only)
        if: matrix.test_mode == 'full' && runner.os == 'Linux'
        timeout-minutes: 10
        run: |
          echo "--- Running ignored Landlock red-team tests (they self-skip when Landlock is unavailable)"
          cargo nextest run --profile ci --locked --no-default-features --run-ignored only \
            -p ahma_mcp \
            -E 'test(red_team_global_read_access_blocked) or test(red_team_livelog_symlink_read_allowed)'

      - name: ARM64 smoke validation (compile-only)
        if: matrix.test_mode == 'smoke'
        timeout-minutes: 25
        run: |
          echo "--- ARM64 smoke: compile-only validation of key crates (no test execution)"
          cargo test --locked --no-default-features -p ahma_bin --no-run
          cargo test --locked --no-default-features -p ahma_http_bridge --no-run
          cargo test --locked --no-default-features -p ahma_task_tree --no-run

      - name: Cargo Clippy
        if: runner.os == 'Linux' && runner.arch == 'X64'
        run: cargo clippy --locked

      - name: Cargo Clippy (no-default-features)
        if: runner.os == 'Linux' && runner.arch == 'X64'
        run: cargo clippy --no-default-features --locked

      - name: Cargo Clippy Tests
        if: runner.os == 'Linux' && runner.arch == 'X64'
        run: cargo clippy --tests --locked

      - name: Cargo Doc
        if: runner.os == 'Linux' && runner.arch == 'X64'
        run: cargo doc --no-deps

      - name: Upload documentation
        if: runner.os == 'Linux' && runner.arch == 'X64'
        uses: actions/upload-artifact@v7
        with:
          name: documentation
          path: target/doc

      - name: Show sccache stats
        if: always() && runner.os != 'Windows'
        run: |
          sccache --show-stats || true

      # Must run on EVERY platform, including Windows (which is excluded from the stats step
      # above): saving while the server still holds the cache open tars a live directory.
      - name: Stop sccache server
        if: always()
        run: sccache --stop-server || true

      # `if: always()` so a red leg still contributes its compilation -- otherwise the retry
      # starts from whatever the last green run left behind.
      - name: Save sccache
        if: always()
        continue-on-error: true # a cache save must never redden a green job
        uses: actions/cache/save@v5
        with:
          path: ${{ github.workspace }}/.sccache
          key: ${{ steps.sccache.outputs.cache-primary-key }}

  #######################################

  job-security:
    name: Security (cargo deny)
    runs-on: ubuntu-latest
    # Run on every push and PR to every branch for earliest CVE detection.
    # This job is intentionally independent of the test matrix.
    env:
      RUSTC_WRAPPER: "" # cargo-deny runs in Docker without sccache; unset the workflow-level wrapper
    steps:
      - uses: actions/checkout@v6
        with:
          ref: ${{ github.event.pull_request.head.sha || github.sha }}
          token: ${{ github.token }}
          persist-credentials: true

      - name: cargo deny check
        uses: EmbarkStudios/cargo-deny-action@v2
        with:
          # Check all four categories: advisories, licenses, bans, sources
          command: check
          # Point at our deny.toml at the workspace root
          manifest-path: Cargo.toml

      - name: Check workspace license boundaries
        run: bash ./scripts/check-license-boundaries.sh

  #######################################

  # Single aggregate gate. This is the ONE required status check on main (rename-stable —
  # individual matrix legs can be renamed without touching branch protection). The merge
  # queue runs this on `merge_group` before merging; a push to main runs it as the release
  # precondition. `if: always()` + explicit result checks make a skipped/failed dependency
  # FAIL the gate (a plain `needs:` would let a skipped dep pass silently).
  ci-green:
    name: CI green
    if: always()
    needs:
      - job-cargo-nextest
      - job-android-nextest
      - job-security
      - job-coverage
    runs-on: ubuntu-latest
    steps:
      - name: Verify all required jobs succeeded
        run: |
          echo "nextest=${{ needs.job-cargo-nextest.result }}"
          echo "android=${{ needs.job-android-nextest.result }}"
          echo "security=${{ needs.job-security.result }}"
          echo "coverage=${{ needs.job-coverage.result }}"
          fail=0
          [ "${{ needs.job-cargo-nextest.result }}" = "success" ] || { echo '::error::test matrix did not pass'; fail=1; }
          [ "${{ needs.job-android-nextest.result }}" = "success" ] || { echo '::error::android tests did not pass'; fail=1; }
          [ "${{ needs.job-security.result }}" = "success" ] || { echo '::error::security (cargo deny) did not pass'; fail=1; }
          # job-coverage only runs on main-branch pushes (its own `if:`), so it is
          # legitimately "skipped" on production pushes / workflow_dispatch. Gate on
          # an actual failure, not on a skip — on the release path (main push) it
          # always runs, so this still blocks a release when coverage fails.
          case "${{ needs.job-coverage.result }}" in
            success|skipped) ;;
            *) echo '::error::coverage did not pass'; fail=1 ;;
          esac
          exit $fail

  #######################################

  job-android-nextest:
    name: Android Nextest
    runs-on: macos-latest
    env:
      TERM: xterm-256color
      # Below the workflow default: this leg shares the quota with the 4-leg test matrix and
      # only builds -p ahma_mcp --features android, so it needs less.
      SCCACHE_CACHE_SIZE: 600M
    steps:
      - uses: actions/checkout@v6
        with:
          ref: ${{ github.event.pull_request.head.sha || github.sha }}
          fetch-depth: 2
          token: ${{ github.token }}
          persist-credentials: true

      - name: Set up JDK 21
        uses: actions/setup-java@v5
        with:
          java-version: "21"
          distribution: "temurin"

      - name: Setup Android SDK
        uses: android-actions/setup-android@v4

      # There are deliberately NO Gradle caches in this job. Two used to exist (`gradle-deeper-*`
      # for ~/.gradle deps and `gradle-android-build-*` for the sample app's build outputs) and
      # BOTH were no-ops for this repo's entire history: every run ended with
      #   Path Validation Error: Path(s) specified in the action for caching do(es) not exist,
      #   hence no cache is being saved.
      # and correspondingly no `gradle-*` entry has ever appeared in `gh cache list`. The paths
      # never materialise because the tests this job runs (`-p ahma_mcp --features android -E
      # 'test(kotlin) or test(android)'`) do not drive a Gradle build of test-data/, so neither
      # ~/.gradle nor app/build/* is ever populated. Restoring could not hit either, since
      # nothing was ever stored. They were pure warning noise plus the false impression that
      # Gradle work was cached.
      #
      # If a real Gradle build is added to this job, re-add caching then -- and verify the save
      # actually stores by checking for a `gradle-*` key in `gh cache list`, not just a green
      # step: the save step succeeds even when it stores nothing.
      #
      # See the registry cache in job-cargo-nextest for why this key has no job-family segment
      # and why registry/src and ~/.cargo/bin are excluded.
      - name: Cache Cargo registry and git
        uses: actions/cache@v5
        with:
          path: |
            ~/.cargo/registry/index
            ~/.cargo/registry/cache
            ~/.cargo/git/db
          key: cargo-registry-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('Cargo.lock') }}
          restore-keys: |
            cargo-registry-${{ runner.os }}-${{ runner.arch }}-

      - name: Restore sccache
        id: sccache
        uses: actions/cache/restore@v5
        with:
          path: ${{ github.workspace }}/.sccache
          key: sccache-android-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('Cargo.lock') }}-${{ github.run_id }}-${{ github.run_attempt }}
          restore-keys: |
            sccache-android-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('Cargo.lock') }}-
            sccache-android-${{ runner.os }}-${{ runner.arch }}-

      - uses: actions-rust-lang/setup-rust-toolchain@v1
        with:
          rustflags: ""
          cache: false

      - name: Install tools
        uses: taiki-e/install-action@v2
        with:
          tool: nextest,sccache,cargo-ndk

      - name: Add Cargo Bin to PATH
        run: echo "$HOME/.cargo/bin" >> "$GITHUB_PATH"

      - name: Run Android/Kotlin nextest only
        run: |
          # Run ONLY kotlin/android tests (feature-gated and name-filtered for clarity)
          cargo nextest run --profile ci --locked -p ahma_mcp --features android -E 'test(kotlin) or test(android)'

      - name: Show sccache stats
        if: always()
        run: |
          sccache --show-stats || true

      - name: Stop sccache server
        if: always()
        run: sccache --stop-server || true

      - name: Save sccache
        if: always()
        continue-on-error: true # a cache save must never redden a green job
        uses: actions/cache/save@v5
        with:
          path: ${{ github.workspace }}/.sccache
          key: ${{ steps.sccache.outputs.cache-primary-key }}

  #######################################

  job-coverage:
    runs-on: ubuntu-latest
    # Only run on main branch pushes, not pull requests
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    env:
      # ghac spike (#562) -- see the section at the end of the caching note at the top of this
      # file. This is deliberately the ONLY switch. `SCCACHE_GHA_VERSION` (the manual purge knob)
      # is not set here and must not be: sccache enables ghac on that variable's mere presence and
      # skips reading SCCACHE_GHA_ENABLED entirely, so setting it would create a second, hidden
      # way for ghac to be on while the line below says it is off.
      #
      # `on` is what sccache's docs use. Unlike SCCACHE_MAX_SIZE in #561, a wrong value here
      # cannot fail silently: `bool_from_env_var` accepts true/on/1/false/off/0 case-insensitively
      # and `bail!`s on anything else, and `Config::load()` propagates that, so sccache refuses to
      # start rather than quietly ignoring the setting.
      SCCACHE_GHA_ENABLED: "on"
      # The workflow-level `SCCACHE_CACHE_SIZE: 1G` is inert here: with ghac configured,
      # `storage_from_config` returns the remote backend and never builds the disk cache, so there
      # is no local directory for a cap to bound. On revert, restore `SCCACHE_CACHE_SIZE: 1300M`.
      #
      # That number is not arbitrary and must not be re-derived from scratch. It MUST stay above
      # this job's live working set, measured at ~1121 MiB. The job compiles two distinct flavours
      # into one cache -- a plain `cargo build -p ahma_bin` plus `cargo llvm-cov nextest`, which
      # adds cfg(coverage) and instrumentation, so those objects are a disjoint (and larger) set
      # rather than a reuse of the plain ones. 800M was measured to be actively harmful: run
      # 31400528803 collapsed to a 2.82% Rust hit rate (from 95.74%) and ran 23% slower, 11.7 ->
      # 14.4 min. Note the trap in diagnosing that -- the overage ratio is NOT the predictor.
      # Coverage was only 1.40x over its cap and collapsed, while the ubuntu test leg was 2.22x
      # over and held 96.90%. What matters is how much of the cache is LIVE: the test legs carry
      # accumulated dead objects from older flag combinations, so eviction discards junk first,
      # whereas nearly all of this job's 1121 MiB is needed by the run that just restored it.
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - uses: actions/checkout@v6
        with:
          ref: ${{ github.event.pull_request.head.sha || github.sha }}
          fetch-depth: 2
          token: ${{ github.token }}
          persist-credentials: true

      # See the registry cache in job-cargo-nextest for why this key has no job-family segment
      # and why registry/src and ~/.cargo/bin are excluded.
      - name: Cache Cargo registry and git
        uses: actions/cache@v5
        with:
          path: |
            ~/.cargo/registry/index
            ~/.cargo/registry/cache
            ~/.cargo/git/db
          key: cargo-registry-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('Cargo.lock') }}
          restore-keys: |
            cargo-registry-${{ runner.os }}-${{ runner.arch }}-

      # There is no Restore/Save sccache pair here any more; ghac writes each object individually
      # as it is compiled (#562). What ghac does need is credentials, and the runner exposes those
      # only to JavaScript actions, not to `run:` steps -- hence github-script + exportVariable.
      #
      # ACTIONS_CACHE_SERVICE_V2 is load-bearing and is NOT in the requirement list in #562.
      # opendal picks the API version from that variable alone (`get_cache_service_version`:
      # non-empty -> V2, empty -> V1) and only V2 reads ACTIONS_RESULTS_URL; V1 tries
      # ACTIONS_CACHE_URL first and talks to the retired `_apis/artifactcache` endpoints. Export
      # the URL and token without it and sccache lands on the dead v1 service, fails every write,
      # degrades to read-only and stays green -- #561's exact shape. So this step asserts on all
      # three inputs rather than defaulting them to "" the way opendal's own README snippet does;
      # an empty ACTIONS_CACHE_URL is worse than an unset one, because V1's `or_else` fallback to
      # ACTIONS_RESULTS_URL only fires when the variable is absent, not when it is set to empty.
      - name: Export Actions cache credentials for sccache (ghac)
        uses: actions/github-script@v9
        with:
          script: |
            const url = process.env.ACTIONS_RESULTS_URL;
            const token = process.env.ACTIONS_RUNTIME_TOKEN;
            if (!url || !token) {
              core.setFailed(
                'ACTIONS_RESULTS_URL / ACTIONS_RUNTIME_TOKEN are not visible to this action, so ' +
                'sccache cannot reach the ghac backend. It would silently fall back to the disk ' +
                'cache. See the ghac section of the caching note in this workflow.');
              return;
            }
            core.setSecret(token);
            core.exportVariable('ACTIONS_RESULTS_URL', url);
            core.exportVariable('ACTIONS_RUNTIME_TOKEN', token);
            // Forced on rather than defaulted off: v1 is decommissioned on github.com, so an
            // absent flag can only mean the runner stopped advertising it, never that v1 is the
            // right choice here. Warn so the reason is in the log if this ever starts happening.
            if (!process.env.ACTIONS_CACHE_SERVICE_V2) {
              core.warning(
                'ACTIONS_CACHE_SERVICE_V2 was not set by the runner; forcing it on so opendal ' +
                'uses the v2 cache service. If ghac reads/writes start failing, check whether ' +
                'the runner renamed this flag.');
            }
            core.exportVariable('ACTIONS_CACHE_SERVICE_V2', process.env.ACTIONS_CACHE_SERVICE_V2 || 'true');

      - uses: actions-rust-lang/setup-rust-toolchain@v1
        with:
          components: llvm-tools-preview
          rustflags: ""
          cache: false

      - name: Configure deterministic tooling env
        run: |
          git config --global core.pager cat
          git config --global fetch.recurseSubmodules false
          {
            echo "PAGER=cat"
            echo "LC_ALL=C"
            echo "LANG=C"
          } >> "$GITHUB_ENV"

      # sccache is version-pinned HERE and nowhere else in this repo, because ghac is the only
      # backend where the version is load-bearing. Two reasons: the ghac backend needs >= v0.11.0
      # (v0.10.0 spoke ghac API v2, v0.11.0 moved to v4 and the docs say earlier versions
      # "probably will not work"), and sccache namespaces its ghac objects as `sccache-v{VERSION}`
      # -- so an unpinned upgrade silently cold-starts the cache and would be read as a spike
      # regression. `@0.17` tracks the 0.17.x series; taiki-e/install-action has no ">=" syntax,
      # so a series pin is how a floor is expressed. The other jobs stay unpinned deliberately:
      # their tarballs do not carry the version in the key.
      - name: Install cargo-nextest, cargo-llvm-cov and sccache
        uses: taiki-e/install-action@v2
        with:
          tool: nextest,cargo-llvm-cov,sccache@0.17

      - name: Install cargo-ndk
        run: |
          [ -x "$HOME/.cargo/bin/cargo-ndk" ] || cargo install cargo-ndk

      - name: Build ahma binary (required by integration tests)
        run: cargo build --locked --no-default-features -p ahma_bin

      - name: Generate coverage report
        timeout-minutes: 40
        run: |
          # Run the instrumented test suite ONCE, deferring report generation so we can
          # emit several report formats from the same profdata without re-running tests.
          cargo llvm-cov nextest --no-report --profile coverage --locked --no-default-features -E 'not (test(kotlin) or test(android))'

          # 1. Full line-by-line HTML for humans drilling into a specific file.
          cargo llvm-cov report --html --output-dir ./coverage

          # 2. Compact machine-readable per-file summary (no per-line trivia) for coverage
          #    planning by tools/agents — see /ahmadev coverage.
          cargo llvm-cov report --json --summary-only --output-path ./coverage/coverage-summary.json

          # 3. Tiny markdown table of the WORST-covered workspace files, sorted ascending by
          #    line coverage. This is the single artifact an LLM should read to plan tests:
          #    it is a few KB and contains no source lines. Paths are made workspace-relative.
          #    Non-fatal: a jq hiccup must never block the HTML/Pages deploy (coverage-summary.json
          #    still carries the same data for /ahmadev coverage to fall back on).
          jq -r --arg ws "$PWD/" '
            "Coverage summary (sorted by line coverage, ascending). Generated from coverage-summary.json.",
            "",
            "Totals: lines \(.data[0].totals.lines.percent | .*100 | round / 100)% (\(.data[0].totals.lines.covered)/\(.data[0].totals.lines.count)), " +
              "functions \(.data[0].totals.functions.percent | .*100 | round / 100)%, " +
              "regions \(.data[0].totals.regions.percent | .*100 | round / 100)%.",
            "",
            "| file | line% | lines (cov/total) | fn% | region% |",
            "|------|------:|-------------------|----:|--------:|",
            ( .data[0].files
              | map({ f: (.filename | sub("^"+$ws; "")),
                      lp: .summary.lines.percent, lcov: .summary.lines.covered, lc: .summary.lines.count,
                      fp: .summary.functions.percent, rp: .summary.regions.percent })
              | sort_by(.lp)[]
              | "| \(.f) | \(.lp*100|round/100) | \(.lcov)/\(.lc) | \(.fp*100|round/100) | \(.rp*100|round/100) |" )
          ' ./coverage/coverage-summary.json > ./coverage/coverage-lowest.md \
            || echo "coverage-lowest.md generation failed (non-fatal); use coverage-summary.json" > ./coverage/coverage-lowest.md

      - name: Generate code simplicity report
        run: |
          cargo run -p ahma_bin --features simplify -- simplify . --limit 20 --html
          cp CODE_SIMPLICITY.md coverage/
          cp CODE_SIMPLICITY.html coverage/

      - name: Generate Rust Docs
        run: |
          cargo doc --no-deps
          cp -r target/doc coverage/
          echo '<meta http-equiv="refresh" content="0; url=ahma_mcp/index.html">' > coverage/doc/index.html

      - name: Setup Pages
        uses: actions/configure-pages@v6

      - name: Upload coverage artifact
        uses: actions/upload-pages-artifact@v5
        with:
          path: ./coverage

      - name: Upload code simplicity report artifact
        uses: actions/upload-artifact@v7
        with:
          name: code-simplicity-report
          path: ./coverage/CODE_SIMPLICITY.md

      - name: Upload compact coverage summary artifact
        uses: actions/upload-artifact@v7
        with:
          name: coverage-summary
          path: |
            ./coverage/coverage-summary.json
            ./coverage/coverage-lowest.md

      - name: Deploy to GitHub Pages
        id: deployment
        # Non-fatal: publishing the coverage/docs report to Pages is a side-artifact, not a
        # release-correctness signal. GitHub's Pages deployment API intermittently returns
        # "Deployment failed, try again later" (a platform-side hiccup, seen even on isolated
        # single pushes with no concurrent deploys). Because ci-green -> job-publish-release
        # depends on this job, a flaky Pages publish was blocking otherwise-green releases.
        # The instrumented test run above still gates the job (a real regression fails it and
        # blocks the release); only the external publish is allowed to flake. The report
        # simply republishes on the next successful main push.
        continue-on-error: true
        uses: actions/deploy-pages@v5

      # The ghac spike's instrument (#562). This DOES fail the job, which is a deliberate break
      # with the `continue-on-error: true # a cache save must never redden a green job` convention
      # used by the tarball steps elsewhere in this file. That convention is right when the cache
      # is pure optimisation; here the cache is the thing under test, and every way ghac can fail
      # leaves the job green (see the ghac section of the caching note). A spike whose failure
      # mode is invisible measures nothing. If this reddens main it is a CI finding, not a code
      # regression: revert the spike as described in that note.
      #
      # Asserted on the JSON form, not the text: `--show-stats` output has no stable grammar, and
      # #561 is the standing reminder that a plausible-looking config that never took effect looks
      # exactly like one that did. `cache_location` is the proof it took effect -- it reads
      # "ghac, name: sccache-vX.Y.Z, prefix: /sccache" only when the remote backend is live, and
      # falls back to a disk path otherwise.
      #
      # Placed after the Pages deploy so a cache verdict never costs the coverage report. Note
      # `sccache --stop-server` is gone with the tarball: cache writes are awaited inside the
      # compile request, so the stats are complete once cargo exits, and there is no live
      # directory left to tar.
      - name: Verify ghac backend and report sccache stats
        if: always()
        run: |
          set -euo pipefail

          # `if: always()` means this also runs when the job died before the toolchain was
          # installed. Report nothing rather than stacking a second, misleading failure on top of
          # the real one. Once sccache exists, every command below is allowed to be fatal.
          if ! command -v sccache >/dev/null 2>&1; then
            echo "sccache is not installed; the job failed before it ran. Nothing to verify."
            exit 0
          fi

          sccache --show-stats
          sccache --show-stats --stats-format=json > sccache-stats.json

          loc=$(jq -r '.cache_location' sccache-stats.json)
          requests=$(jq -r '.stats.compile_requests' sccache-stats.json)
          writes=$(jq -r '.stats.cache_writes' sccache-stats.json)
          timeouts=$(jq -r '.stats.cache_timeouts' sccache-stats.json)
          # cache_errors is a PerLanguageCount (a map), the other two are scalars.
          errors=$(jq -r '.stats.cache_read_errors + .stats.cache_write_errors
                          + ([.stats.cache_errors.counts[]] | add // 0)' sccache-stats.json)
          # "Rust" is sccache's own language key (CompilerKind::lang_kind), so these are directly
          # comparable to the "Cache hits rate (Rust)" rows in the #562/#563 baseline tables.
          hits=$(jq -r '.stats.cache_hits.counts.Rust // 0' sccache-stats.json)
          misses=$(jq -r '.stats.cache_misses.counts.Rust // 0' sccache-stats.json)
          rate=$(awk -v h="$hits" -v m="$misses" 'BEGIN { t=h+m; printf "%.2f", (t ? 100*h/t : 0) }')

          {
            echo "### sccache (ghac spike, #562)"
            echo
            echo "| metric | value |"
            echo "|---|---|"
            echo "| backend | \`$loc\` |"
            echo "| compile requests | $requests |"
            echo "| Rust hit rate | ${rate}% ($hits hit / $misses miss) |"
            echo "| cache writes | $writes |"
            echo "| cache errors | $errors |"
            echo "| cache timeouts | $timeouts |"
            echo
            echo "Tarball baseline (run 31404259524): 80.69% Rust, 11.7 min. Compare WALL CLOCK."
          } >> "$GITHUB_STEP_SUMMARY"

          failed=0

          case "$loc" in
            ghac*) ;;
            *)
              echo "::error::sccache is not using the ghac backend (cache_location: $loc). SCCACHE_GHA_ENABLED did not take effect, so this run measured the disk cache."
              failed=1
              ;;
          esac

          if [ "$errors" -ne 0 ]; then
            echo "::error::sccache reported $errors cache errors. Under ghac this is how a rate-limited or credential-less backend surfaces: it degrades to read-only and every put returns 'Cannot write to read-only storage'."
            failed=1
          fi

          # The residual silent case: read-only reached some other way, so nothing is ever
          # written and nothing is ever counted as an error either. Misses without writes is the
          # signature. Guarded on misses so a 100%-hit run is not treated as a failure.
          if [ "$misses" -gt 0 ] && [ "$writes" -eq 0 ]; then
            echo "::error::sccache recorded $misses Rust misses but wrote 0 objects. The ghac backend is not storing anything."
            failed=1
          fi

          # Reported, not fatal. A timeout is a miss caused by a slow cache, which is exactly what
          # throttling looks like -- but it is also the one signal that can move for reasons
          # outside this repo, and reddening main on it would be flaky. It shows in the summary
          # table above; if it climbs across runs, that is the spike failing.
          if [ "$timeouts" -ne 0 ]; then
            echo "::warning::sccache reported $timeouts cache timeouts. Under ghac this is the throttling signal; watch it across runs."
          fi

          exit "$failed"

  #######################################

  # Release gate: the version number is the release trigger. This cheap job (~10s) decides
  # ONCE whether the version in Cargo.toml has already been released, and gates the expensive
  # 6-platform release build + publish below. A push to main that does NOT bump the version
  # (an ordinary land) therefore skips the release builds entirely — it only runs the test
  # matrix + coverage. Release binaries are built (and Sigstore-attested) exactly once per
  # version, at the bump that ships it, so there are never two different binaries claiming the
  # same version.
  job-release-gate:
    name: Release Gate
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    outputs:
      should_release: ${{ steps.check.outputs.should_release }}
      tag: ${{ steps.check.outputs.tag }}
    steps:
      - uses: actions/checkout@v6
        with:
          ref: ${{ github.event.pull_request.head.sha || github.sha }}
          token: ${{ github.token }}
          persist-credentials: true

      - name: Decide whether this version is unreleased
        id: check
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          VERSION=$(grep '^version' Cargo.toml | head -1 | sed -E 's/^version = "(.*)"/\1/')
          TAG="v${VERSION}"
          echo "tag=${TAG}" >> "$GITHUB_OUTPUT"
          if gh release view "${TAG}" --json tagName > /dev/null 2>&1; then
            echo "should_release=false" >> "$GITHUB_OUTPUT"
            echo "Release ${TAG} already exists — skipping release builds for this push."
          else
            echo "should_release=true" >> "$GITHUB_OUTPUT"
            echo "Release ${TAG} not found — this push will build and publish it."
          fi

  #######################################

  job-release-binaries:
    name: Release Binaries (${{ matrix.platform }})
    # Run only on main pushes that bump to an as-yet-unreleased version (see job-release-gate).
    needs: job-release-gate
    if: needs.job-release-gate.outputs.should_release == 'true'
    permissions:
      id-token: write      # for Sigstore OIDC token (GitHub attestation)
      attestations: write  # to write SLSA provenance to GitHub's attestation API
      contents: read       # for checkout
    defaults:
      run:
        shell: bash
    strategy:
      fail-fast: false
      # 6 release targets; run them all concurrently. At max-parallel:2 the musl-arm64 tail
      # didn't start until +22 min and set the ~31-min post-merge time on its own.
      max-parallel: 6
      matrix:
        include:
          # Native builds (no cross-compilation needed)
          - os: ubuntu-latest
            target: x86_64-unknown-linux-gnu
            platform: linux-x86_64
            use_cross: false
          - os: macos-latest
            target: aarch64-apple-darwin
            platform: darwin-arm64
            use_cross: false
          # Windows native build
          - os: windows-latest
            target: x86_64-pc-windows-msvc
            platform: windows-x86_64
            use_cross: false
          # Cross-compiled aarch64 glibc build.
          # We deliberately do NOT build natively on `ubuntu-24.04-arm` because
          # GitHub's hosted ARM64 runners terminate during the heavy release
          # compilation of this workspace (e.g. `ahma_mcp`), see commit 9a315fc
          # and the failure of run 26750268775 for prior evidence.
          - os: ubuntu-latest
            target: aarch64-unknown-linux-gnu
            platform: linux-arm64
            use_cross: true
          # Cross-compiled musl builds (portable, no glibc dependency)
          - os: ubuntu-latest
            target: x86_64-unknown-linux-musl
            platform: linux-x86_64-musl
            use_cross: true
          - os: ubuntu-latest
            target: aarch64-unknown-linux-musl
            platform: linux-arm64-musl
            use_cross: true
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v6
        with:
          ref: ${{ github.event.pull_request.head.sha || github.sha }}
          fetch-depth: 2
          token: ${{ github.token }}
          persist-credentials: true

      # Keeping antivirus out of the build tree is a speed optimization, not a
      # correctness requirement. Add-MpPreference is a live call to the Defender
      # service, which on hosted runners is sometimes disabled or unreachable
      # (WMI 0x800106ba) -- so it must never be able to fail the job.
      - name: Configure Windows Defender exclusions (best effort)
        if: runner.os == 'Windows'
        shell: powershell
        continue-on-error: true
        run: |
          $paths = @("${{ github.workspace }}", "$env:USERPROFILE\.cargo", "$env:TEMP")
          foreach ($path in $paths) {
            try {
              Add-MpPreference -ExclusionPath $path -ErrorAction Stop
              Write-Host "Defender exclusion added: $path"
            } catch {
              # Collapse newlines: a raw newline truncates a ::warning:: annotation.
              $reason = ($_.Exception.Message -replace '\s+', ' ').Trim()
              Write-Host "::warning::Defender exclusion skipped for ${path}: ${reason} -- builds may be slower, but correctness is unaffected."
            }
          }

      # Registry only, and NOT keyed on matrix.target: the registry is host-side, identical for
      # every target built on a given runner, so the 4 ubuntu legs share one entry (previously
      # 6 near-identical copies, ~0.8 GiB). See job-cargo-nextest for the path exclusions.
      #
      # Expect "Unable to reserve cache with key ..., another job may be creating this cache"
      # warnings here on a release run: this job needs only job-release-gate (~10s), so the 4
      # ubuntu legs start alongside the ubuntu test legs and race to save the now-shared key.
      # It is benign -- the loser skips the upload and the step still succeeds, and whichever
      # wins writes identical content, since the key already pins (Cargo.lock, OS, arch). That
      # content-independence is the whole reason one shared registry entry is safe.
      #
      # There is deliberately NO sccache cache here -- rule 2 in the caching note at the top of
      # this file. Release legs build with `--release`, so they share zero sccache objects with
      # the test matrix, they run only on a version bump, and they are off the critical path;
      # their 3 entries cost ~1.2 GiB of a 10 GiB quota that every merge competes for.
      # RUSTC_WRAPPER stays set so the stats step below still reports, but nothing is uploaded.
      - name: Cache Cargo registry and git
        uses: actions/cache@v5
        with:
          path: |
            ~/.cargo/registry/index
            ~/.cargo/registry/cache
            ~/.cargo/git/db
          key: cargo-registry-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('Cargo.lock') }}
          restore-keys: |
            cargo-registry-${{ runner.os }}-${{ runner.arch }}-

      - uses: actions-rust-lang/setup-rust-toolchain@v1
        with:
          target: ${{ matrix.target }}
          rustflags: ""
          cache: false

      - name: Configure deterministic tooling env
        run: |
          git config --global core.pager cat
          git config --global fetch.recurseSubmodules false
          {
            echo "PAGER=cat"
            echo "LC_ALL=C"
            echo "LANG=C"
          } >> "$GITHUB_ENV"

      - name: Preflight workspace crate roots
        run: |
          set -euo pipefail
          missing=0
          while IFS= read -r manifest; do
            crate_dir="$(dirname "$manifest")"
            # Workspace root manifests may not define a package.
            if ! grep -q "^\[package\]" "$manifest"; then
              continue
            fi
            if [ ! -f "$crate_dir/src/lib.rs" ] && [ ! -f "$crate_dir/src/main.rs" ]; then
              echo "Missing crate root in $crate_dir (expected src/lib.rs or src/main.rs)"
              missing=1
            fi
          done < <(find . -name Cargo.toml -not -path "./target/*")

          if [ "$missing" -ne 0 ]; then
            echo ""
            echo "Preflight failed: one or more crates are missing source roots."
            echo "This often happens when required files exist locally but are untracked."
            exit 1
          fi

      - name: Install sccache
        if: "!matrix.use_cross"
        uses: taiki-e/install-action@v2
        with:
          tool: sccache

      - name: Add Cargo Bin to PATH (macOS)
        if: "!matrix.use_cross && runner.os == 'macOS'"
        run: echo "$HOME/.cargo/bin" >> "$GITHUB_PATH"

      - name: Install cross (for cross-compilation)
        if: matrix.use_cross
        uses: taiki-e/install-action@v2
        with:
          tool: cross

      - name: Build release binaries (native)
        if: "!matrix.use_cross"
        run: |
          cargo build --release --locked --target ${{ matrix.target }} -p ahma_bin

      - name: Build release binaries (cross-compiled)
        if: matrix.use_cross
        run: |
          # Disable sccache for cross builds (incompatible with cross's Docker containers)
          unset RUSTC_WRAPPER
          cross build --release --locked --target ${{ matrix.target }} -p ahma_bin

      - name: Package release binaries
        id: package
        run: |
          set -euo pipefail
          platform="${{ matrix.platform }}"
          mkdir -p dist
          if [[ "$RUNNER_OS" == "Windows" ]]; then
            archive_name="ahma-release-${platform}.zip"
            cp "target/${{ matrix.target }}/release/ahma.exe" dist/
            cp README.md dist/
            (cd dist && sha256sum ahma.exe > SHA256SUMS)
            (cd dist && 7z a "../${archive_name}" ahma.exe SHA256SUMS README.md)
          else
            archive_name="ahma-release-${platform}.tar.gz"
            cp target/${{ matrix.target }}/release/ahma dist/
            cp README.md dist/
            (cd dist && shasum -a 256 ahma > SHA256SUMS)
            tar -czf "$archive_name" -C dist ahma SHA256SUMS README.md
          fi
          echo "archive_name=$archive_name" >> "$GITHUB_OUTPUT"
          echo "platform=$platform" >> "$GITHUB_OUTPUT"

      - name: Upload release binaries artifact
        uses: actions/upload-artifact@v7
        with:
          name: release-binaries-${{ matrix.platform }}
          path: ${{ steps.package.outputs.archive_name }}

      - name: Attest build provenance
        uses: actions/attest-build-provenance@v2
        with:
          subject-path: |
            ${{ steps.package.outputs.archive_name }}
            dist/ahma*

      - name: Show sccache stats
        if: always() && !matrix.use_cross && runner.os != 'Windows'
        run: |
          sccache --show-stats || true
          sccache --stop-server || true

  #######################################

  job-publish-release:
    name: Publish Release
    # Gate the release on correctness (ci-green already aggregates nextest + android +
    # security) plus the actual artifacts (release-binaries). Coverage is a Pages/reporting
    # job (~8 min) and is intentionally NOT on the release critical path anymore — it runs
    # in parallel without blocking the publish.
    needs:
      - ci-green
      - job-release-gate
      - job-release-binaries
    if: needs.job-release-gate.outputs.should_release == 'true'
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v6
        with:
          ref: ${{ github.event.pull_request.head.sha || github.sha }}
          token: ${{ github.token }}
          persist-credentials: true

      - name: Download all release artifacts
        uses: actions/download-artifact@v8
        with:
          pattern: release-binaries-*
          merge-multiple: true

      - name: Generate combined SHA256SUMS
        run: |
          # 1. Produce a single SHA256SUMS file covering every release archive.
          sha256sum ahma-release-*.tar.gz ahma-release-*.zip > SHA256SUMS

          # 2. Extract the individual binary hashes from the archives and append them
          # to the combined SHA256SUMS file. This allows verifying the installed binary
          # directly without downloading the archive again.
          echo "" >> SHA256SUMS
          echo "# Raw Binaries" >> SHA256SUMS
          for f in ahma-release-*.tar.gz; do
            if [ -f "$f" ]; then
              platform=$(echo "$f" | sed -E 's/ahma-release-(.*)\.tar\.gz/\1/')
              hash=$(tar -xOf "$f" SHA256SUMS | grep -E "ahma$" | head -n 1 | awk '{print $1}')
              echo "$hash  ahma-${platform}" >> SHA256SUMS
            fi
          done
          for f in ahma-release-*.zip; do
            if [ -f "$f" ]; then
              platform=$(echo "$f" | sed -E 's/ahma-release-(.*)\.zip/\1/')
              hash=$(unzip -p "$f" SHA256SUMS | grep "ahma.exe" | head -n 1 | awk '{print $1}')
              echo "$hash  ahma-${platform}.exe" >> SHA256SUMS
            fi
          done

          echo "Combined SHA256SUMS contents:"
          cat SHA256SUMS

      - name: Determine release tag
        id: version
        run: |
          VERSION=$(grep '^version' Cargo.toml | head -1 | sed -E 's/^version = "(.*)"/\1/')
          TAG="v${VERSION}"
          echo "version=${VERSION}" >> "$GITHUB_OUTPUT"
          echo "tag=${TAG}" >> "$GITHUB_OUTPUT"
          # Skip if this exact version was already published
          if gh release view "${TAG}" --json tagName > /dev/null 2>&1; then
            echo "already_released=true" >> "$GITHUB_OUTPUT"
            echo "Release ${TAG} already exists — skipping."
          else
            echo "already_released=false" >> "$GITHUB_OUTPUT"
          fi
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Create Release
        if: steps.version.outputs.already_released == 'false'
        uses: softprops/action-gh-release@v3
        with:
          tag_name: ${{ steps.version.outputs.tag }}
          name: ${{ steps.version.outputs.tag }}
          body: |
            ## Ahma ${{ steps.version.outputs.tag }}

            Built from [`main`](https://github.com/paulirotta/ahma/tree/main) via the [CI pipeline](https://github.com/paulirotta/ahma/actions/workflows/build.yml).

            ### Install

            **Linux / macOS:**
            ```bash
            curl -sSf https://raw.githubusercontent.com/paulirotta/ahma/main/scripts/install.sh | bash
            export PATH="$HOME/.local/bin:$PATH"
            ```

            **Windows (PowerShell 5.1+):**
            ```powershell
            irm https://raw.githubusercontent.com/paulirotta/ahma/main/scripts/install.ps1 | iex
            ```

            **Update an existing install:**
            ```bash
            ahma update
            ```

            ### Supply chain protection — SLSA Level 3 via Sigstore

            Every release archive and raw binary has a **GitHub Build Provenance Attestation** (Sigstore).
            Attestations are keyless — signed by an ephemeral Fulcio certificate bound to this workflow's
            OIDC identity. No private key exists; no rotation is needed.

            **`ahma update` and `ahma verify --self` verify the attestation automatically.**

            **Manual out-of-band verification:**
            ```bash
            gh attestation verify ahma-release-linux-x86_64.tar.gz \
              --repo paulirotta/ahma
            ```

            ### AGPL + SLSA: two-layer supply chain defence

            The `ahma` binary and security-relevant crates are **AGPL-3.0-or-later**.
            AGPL requires source disclosure for any distributed or network-accessible modification,
            closing the route of shipping a backdoored binary without publishing the changes.
            The Sigstore attestation verifies that what you install was built from the published,
            auditable source by the official CI pipeline.

            Building from auditable source is always an option:
            ```bash
            cargo install --git https://github.com/paulirotta/ahma ahma_bin --bin ahma --root ~/.local --locked
            ```

            See [docs/release-signing.md](https://github.com/paulirotta/ahma/blob/main/docs/release-signing.md)
            for the trust model and verification details.
          draft: false
          prerelease: false
          make_latest: "true"
          files: |
            ahma-release-*.tar.gz
            ahma-release-*.zip
            SHA256SUMS

