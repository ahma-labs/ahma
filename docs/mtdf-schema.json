{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "ToolConfig",
  "description": "Represents the complete configuration for a command-line tool.",
  "type": "object",
  "properties": {
    "availability_check": {
      "description": "Runtime availability probe executed at server startup.",
      "anyOf": [
        {
          "$ref": "#/$defs/AvailabilityCheck"
        },
        {
          "type": "null"
        }
      ]
    },
    "command": {
      "type": "string"
    },
    "decompose": {
      "description": "Decompose orchestration configuration. Required when `tool_type` is `Decompose`.",
      "anyOf": [
        {
          "$ref": "#/$defs/DecomposeConfig"
        },
        {
          "type": "null"
        }
      ]
    },
    "description": {
      "type": "string"
    },
    "enabled": {
      "type": "boolean",
      "default": true
    },
    "guidance_key": {
      "description": "Key to look up hardcoded guidance",
      "type": [
        "string",
        "null"
      ]
    },
    "hints": {
      "$ref": "#/$defs/ToolHints",
      "default": {}
    },
    "input_schema": {
      "description": "Generated input schema (optional - auto-generated from subcommands)"
    },
    "install_instructions": {
      "description": "Installation guidance displayed when the tool is unavailable.",
      "type": [
        "string",
        "null"
      ]
    },
    "livelog": {
      "description": "Live log monitoring configuration. Required when `tool_type` is `Livelog`.",
      "anyOf": [
        {
          "$ref": "#/$defs/LivelogConfig"
        },
        {
          "type": "null"
        }
      ]
    },
    "monitor_level": {
      "description": "Default log monitor level for this tool. When set, all invocations of this tool\nwill have live log monitoring enabled at this severity.\nValues: \"error\", \"warn\", \"info\", \"debug\", \"trace\"",
      "type": [
        "string",
        "null"
      ]
    },
    "monitor_stream": {
      "description": "Which stream to monitor for log patterns (default: \"stderr\").\nValues: \"stderr\", \"stdout\", \"both\"",
      "type": [
        "string",
        "null"
      ]
    },
    "name": {
      "type": "string"
    },
    "sequence": {
      "description": "Optional sequence of tools to execute in order (for composite tools)",
      "type": [
        "array",
        "null"
      ],
      "items": {
        "$ref": "#/$defs/SequenceStep"
      }
    },
    "step_delay_ms": {
      "description": "Delay in milliseconds between sequence steps (default: SEQUENCE_STEP_DELAY_MS)",
      "type": [
        "integer",
        "null"
      ],
      "format": "uint64",
      "minimum": 0
    },
    "subcommand": {
      "type": [
        "array",
        "null"
      ],
      "items": {
        "$ref": "#/$defs/SubcommandConfig"
      }
    },
    "synchronous": {
      "description": "Override the default execution mode for this tool.\n- `true`: Always run synchronously (blocking, returns result immediately)\n- `false`: Always run asynchronously (non-blocking, returns operation ID)\n- `null`/omitted: Use server default (async unless --sync CLI flag)\n\nInheritance: Subcommand-level settings override tool-level settings.",
      "type": [
        "boolean",
        "null"
      ]
    },
    "timeout_seconds": {
      "description": "Default timeout for operations in seconds",
      "type": [
        "integer",
        "null"
      ],
      "format": "uint64",
      "minimum": 0
    },
    "tool_type": {
      "description": "Tool type classifier. Defaults to `Command` for normal CLI tools.\nSet to `Livelog` for long-running log-streaming tools that pipe output through an LLM.",
      "anyOf": [
        {
          "$ref": "#/$defs/ToolType"
        },
        {
          "type": "null"
        }
      ]
    },
    "worker": {
      "description": "Worker synthesis configuration. Required when `tool_type` is `Worker`.",
      "anyOf": [
        {
          "$ref": "#/$defs/WorkerConfig"
        },
        {
          "type": "null"
        }
      ]
    }
  },
  "additionalProperties": false,
  "required": [
    "name",
    "description",
    "command"
  ],
  "$defs": {
    "AvailabilityCheck": {
      "description": "Defines how to probe for tool or subcommand availability at runtime.",
      "type": "object",
      "properties": {
        "args": {
          "description": "Arguments passed to the availability probe.",
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "command": {
          "description": "Optional override for the executable to invoke during the check. Defaults to the tool command.",
          "type": [
            "string",
            "null"
          ]
        },
        "skip_subcommand_args": {
          "description": "If true, do not append derived subcommand arguments when constructing the probe command.",
          "type": "boolean"
        },
        "success_exit_codes": {
          "description": "Exit codes considered successful (defaults to `[0]`).",
          "type": [
            "array",
            "null"
          ],
          "items": {
            "type": "integer",
            "format": "int32"
          }
        },
        "working_directory": {
          "description": "Working directory used for the probe (defaults to project root).",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "additionalProperties": false
    },
    "CommandOption": {
      "description": "Configuration for a single command-line option.",
      "type": "object",
      "properties": {
        "alias": {
          "type": [
            "string",
            "null"
          ]
        },
        "description": {
          "type": [
            "string",
            "null"
          ]
        },
        "file_arg": {
          "type": [
            "boolean",
            "null"
          ]
        },
        "file_flag": {
          "type": [
            "string",
            "null"
          ]
        },
        "format": {
          "type": [
            "string",
            "null"
          ]
        },
        "items": {
          "anyOf": [
            {
              "$ref": "#/$defs/ItemsSpec"
            },
            {
              "type": "null"
            }
          ]
        },
        "name": {
          "type": "string"
        },
        "required": {
          "type": [
            "boolean",
            "null"
          ]
        },
        "type": {
          "type": "string"
        }
      },
      "additionalProperties": false,
      "required": [
        "name",
        "type"
      ]
    },
    "DecomposeConfig": {
      "description": "Configuration for a `tool_type: decompose` tool.\n\nA decompose tool splits a complex business question into smaller sub-questions,\ndispatches each to a local LLM (e.g. `gemma:4b` via Ollama), and aggregates\nthe results with a deterministic Rust reducer — no cloud egress required.",
      "type": "object",
      "properties": {
        "answer_prompt": {
          "description": "System prompt injected when asking the LLM to answer each sub-question.\nIf absent, a generic \"answer concisely\" prompt is used.",
          "type": [
            "string",
            "null"
          ]
        },
        "llm_provider": {
          "description": "LLM provider to use for both splitting and answering sub-questions.\nPrefer a small local model (`gemma:4b`, `llama3.2:3b`) for privacy.",
          "$ref": "#/$defs/LlmProviderConfig"
        },
        "llm_timeout_seconds": {
          "description": "Timeout in seconds for each individual LLM call.  Defaults to 30.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint64",
          "minimum": 0
        },
        "max_concurrent": {
          "description": "Maximum number of sub-questions to run concurrently.\nKeep this low (2–4) for single-machine Ollama deployments.  Defaults to 3.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint",
          "minimum": 0
        },
        "max_subtasks": {
          "description": "Maximum number of sub-questions to generate from the parent question.\nDefaults to 5.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint",
          "minimum": 0
        },
        "reduce_mode": {
          "description": "How to combine sub-task results.\nValues: `summarize` (default), `extract_fields`, `classify`, `concat`, `first`.",
          "anyOf": [
            {
              "$ref": "#/$defs/ReduceMode"
            },
            {
              "type": "null"
            }
          ]
        }
      },
      "required": [
        "llm_provider"
      ]
    },
    "ItemsSpec": {
      "description": "Schema details for array items.",
      "type": "object",
      "properties": {
        "description": {
          "type": [
            "string",
            "null"
          ]
        },
        "format": {
          "type": [
            "string",
            "null"
          ]
        },
        "type": {
          "type": "string"
        }
      },
      "additionalProperties": false,
      "required": [
        "type"
      ]
    },
    "LivelogConfig": {
      "description": "Configuration for a `tool_type: livelog` tool.\n\nA livelog tool spawns a long-running source command (e.g. `adb logcat`),\naccumulates output into chunks, and periodically asks an LLM whether the chunk\ncontains issues matching the `detection_prompt`.  When an issue is found, a\n`ProgressUpdate::LogAlert` notification is pushed to the MCP client.",
      "type": "object",
      "properties": {
        "chunk_max_lines": {
          "description": "Maximum number of lines to accumulate before sending a chunk to the LLM.\nDefaults to 50.",
          "type": "integer",
          "format": "uint",
          "default": 50,
          "minimum": 0
        },
        "chunk_max_seconds": {
          "description": "Maximum time in seconds to wait before sending a partial chunk to the LLM.\nDefaults to 30.",
          "type": "integer",
          "format": "uint64",
          "default": 30,
          "minimum": 0
        },
        "cooldown_seconds": {
          "description": "Minimum time in seconds between consecutive LLM alerts for this tool.\nPrevents alert storms when many lines match in quick succession.  Defaults to 60.",
          "type": "integer",
          "format": "uint64",
          "default": 60,
          "minimum": 0
        },
        "detection_prompt": {
          "description": "Plain-English description of what to look for, passed to the LLM as the\ndetection criteria (e.g. \"Look for crashes, exceptions, or ANR errors\").",
          "type": "string"
        },
        "llm_provider": {
          "description": "LLM provider connection details.",
          "$ref": "#/$defs/LlmProviderConfig"
        },
        "llm_timeout_seconds": {
          "description": "Maximum time in seconds to wait for the LLM to respond to a single request.\nDefaults to 30.",
          "type": "integer",
          "format": "uint64",
          "default": 30,
          "minimum": 0
        },
        "source_args": {
          "description": "Arguments for the source command (e.g. `[\"logcat\", \"-v\", \"threadtime\"]`).",
          "type": "array",
          "default": [],
          "items": {
            "type": "string"
          }
        },
        "source_command": {
          "description": "The executable to run as the log source (e.g. `\"adb\"`, `\"ssh\"`, `\"tail\"`).",
          "type": "string"
        }
      },
      "required": [
        "source_command",
        "detection_prompt",
        "llm_provider"
      ]
    },
    "LlmProviderConfig": {
      "description": "Connection details for an OpenAI-compatible LLM provider.",
      "type": "object",
      "properties": {
        "api_key": {
          "description": "Optional bearer token. Omit for local models that don't require authentication.",
          "type": [
            "string",
            "null"
          ]
        },
        "base_url": {
          "description": "Base URL of the API endpoint, e.g. `http://localhost:11434/v1` (Ollama)\nor `https://api.openai.com/v1`.",
          "type": "string"
        },
        "model": {
          "description": "Model identifier, e.g. `llama3.2`, `gpt-4o-mini`.",
          "type": "string"
        }
      },
      "required": [
        "base_url",
        "model"
      ]
    },
    "ReduceMode": {
      "description": "How to combine sub-task results into a single answer.",
      "oneOf": [
        {
          "description": "Concatenate each result under a numbered heading.",
          "type": "string",
          "const": "summarize"
        },
        {
          "description": "Collect unique key=value pairs extracted from each result.",
          "type": "string",
          "const": "extract_fields"
        },
        {
          "description": "Majority-vote classification: pick the label that appears most often.",
          "type": "string",
          "const": "classify"
        },
        {
          "description": "Append all non-empty results separated by double newlines.",
          "type": "string",
          "const": "concat"
        },
        {
          "description": "Return only the first non-empty result (useful for \"find first match\").",
          "type": "string",
          "const": "first"
        }
      ]
    },
    "SequenceStep": {
      "description": "Represents a single step in a tool sequence",
      "type": "object",
      "properties": {
        "args": {
          "description": "Arguments to pass to the tool",
          "type": "object",
          "additionalProperties": true,
          "default": {}
        },
        "description": {
          "description": "Optional description for logging/display",
          "type": [
            "string",
            "null"
          ]
        },
        "skip_if_file_exists": {
          "description": "Skip this step if the specified file exists",
          "type": [
            "string",
            "null"
          ]
        },
        "skip_if_file_missing": {
          "description": "Skip this step if the specified file is missing",
          "type": [
            "string",
            "null"
          ]
        },
        "subcommand": {
          "description": "Subcommand within that tool",
          "type": "string"
        },
        "tool": {
          "description": "Name of the tool to invoke",
          "type": "string"
        }
      },
      "additionalProperties": false,
      "required": [
        "tool",
        "subcommand"
      ]
    },
    "SubcommandConfig": {
      "description": "Configuration for a subcommand, allowing for nested commands.",
      "type": "object",
      "properties": {
        "availability_check": {
          "description": "Runtime availability probe executed at server startup.",
          "anyOf": [
            {
              "$ref": "#/$defs/AvailabilityCheck"
            },
            {
              "type": "null"
            }
          ]
        },
        "description": {
          "type": "string"
        },
        "enabled": {
          "type": "boolean",
          "default": true
        },
        "guidance_key": {
          "description": "Key to look up hardcoded guidance",
          "type": [
            "string",
            "null"
          ]
        },
        "install_instructions": {
          "description": "Installation guidance displayed when the subcommand is unavailable.",
          "type": [
            "string",
            "null"
          ]
        },
        "name": {
          "type": "string"
        },
        "options": {
          "type": [
            "array",
            "null"
          ],
          "items": {
            "$ref": "#/$defs/CommandOption"
          }
        },
        "positional_args": {
          "description": "Optional arguments that are not flags, but positional values.",
          "type": [
            "array",
            "null"
          ],
          "items": {
            "$ref": "#/$defs/CommandOption"
          }
        },
        "positional_args_first": {
          "description": "When true, positional args are placed BEFORE options in the command line.\nRequired for commands like `find` where path must precede expressions.\nDefault: false (options come first, then positional args)",
          "type": [
            "boolean",
            "null"
          ]
        },
        "sequence": {
          "description": "Optional sequence of tools to execute in order (for composite tools)",
          "type": [
            "array",
            "null"
          ],
          "items": {
            "$ref": "#/$defs/SequenceStep"
          }
        },
        "step_delay_ms": {
          "description": "Delay in milliseconds between sequence steps (default: SEQUENCE_STEP_DELAY_MS)",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint64",
          "minimum": 0
        },
        "subcommand": {
          "type": [
            "array",
            "null"
          ],
          "items": {
            "$ref": "#/$defs/SubcommandConfig"
          }
        },
        "synchronous": {
          "description": "Override the default execution mode for this subcommand.\n- `true`: Always run synchronously (blocking, returns result immediately)\n- `false`: Always run asynchronously (non-blocking, returns operation ID)\n- `null`/omitted: Inherit from tool level, or use server default if tool doesn't specify\n\nInheritance: Subcommand-level settings override tool-level settings.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "timeout_seconds": {
          "description": "Override timeout for this specific subcommand",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint64",
          "minimum": 0
        }
      },
      "additionalProperties": false,
      "required": [
        "name",
        "description"
      ]
    },
    "ToolHints": {
      "description": "Provides hints to an AI agent on how to use a tool.",
      "type": "object",
      "properties": {
        "build": {
          "type": [
            "string",
            "null"
          ]
        },
        "clean": {
          "type": [
            "string",
            "null"
          ]
        },
        "custom": {
          "type": [
            "object",
            "null"
          ],
          "additionalProperties": {
            "type": "string"
          }
        },
        "dependencies": {
          "type": [
            "string",
            "null"
          ]
        },
        "run": {
          "type": [
            "string",
            "null"
          ]
        },
        "test": {
          "type": [
            "string",
            "null"
          ]
        }
      },
      "additionalProperties": false
    },
    "ToolType": {
      "description": "Classifier that determines how the MCP service routes a tool invocation.",
      "oneOf": [
        {
          "description": "Normal command-line tool (default).",
          "type": "string",
          "const": "command"
        },
        {
          "description": "Long-running log source piped through an LLM for issue detection.",
          "type": "string",
          "const": "livelog"
        },
        {
          "description": "Decompose a complex question into sub-questions, dispatch to local LLMs,\nand aggregate the answers with a deterministic reducer.",
          "type": "string",
          "const": "decompose"
        },
        {
          "description": "Synthesize and run ephemeral Rust or Python worker code in a sub-vault.",
          "type": "string",
          "const": "worker"
        }
      ]
    },
    "WorkerConfig": {
      "description": "Configuration for a `tool_type: worker` tool.\n\nA worker tool accepts synthesized source code, compiles or runs it inside a\nsub-vault, captures the output to `outputs/`, and by default deletes the\nsource after execution.  Because the program is compiled deterministic code\n(not an LLM in the loop), it cannot be re-injected mid-run.",
      "type": "object",
      "properties": {
        "extra_args": {
          "description": "Additional compiler / interpreter arguments.",
          "type": [
            "array",
            "null"
          ],
          "items": {
            "type": "string"
          }
        },
        "keep_source": {
          "description": "Keep the synthesized source file after execution (`--keep` flag semantic).\nDefaults to `false` — source is deleted after the run.",
          "type": "boolean",
          "default": false
        },
        "language": {
          "description": "Programming language for the synthesized worker.",
          "$ref": "#/$defs/WorkerLanguage",
          "default": "rust"
        },
        "timeout_seconds": {
          "description": "Execution timeout in seconds.  Defaults to 60.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint64",
          "minimum": 0
        }
      }
    },
    "WorkerLanguage": {
      "description": "Language for an ephemeral synthesized worker.",
      "oneOf": [
        {
          "description": "Compile and run a Rust program (`rustc` must be on PATH).",
          "type": "string",
          "const": "rust"
        },
        {
          "description": "Run a Python 3 script (`python3` must be on PATH).",
          "type": "string",
          "const": "python"
        }
      ]
    }
  }
}